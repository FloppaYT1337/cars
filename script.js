var bottom = 21;
var left = 20;

if (bottom > left) print('Bottom is bigger than left! Reducing');
wait(3);
bottom = bottom - 2;

if (bottom < left) print('well done');
end;

if (bottom = left)
 print("everything seems normal")
end;
